/**
 * 2 * sum / n = 2 * n * n+1
 * an = n
 */
#include <iostream>
using namespace std;
int main(void)
{
    int T;
    long long num;
    long long ans;
    cin >> T;

    for (int i = 0; i < T; i++)
    {
        cin >> num;
        cout << num << endl;
        for (int i = 0; i < num; i++)
        {
            int i, j, k, l, m, n;
        }
    }

    return 0;
}